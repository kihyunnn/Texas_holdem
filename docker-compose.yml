version: '3.8'

services:
  web:
    build: .
    container_name: texas_holdem_tracker
    ports:
      - "5000:5000"
    volumes:
      - ./poker.db:/app/poker.db
    env_file:
      - .env
    restart: unless-stopped
    # Gunicorn command is defined in Dockerfile
